node_modules/
dist/
__pycache__/
package-lock.json
.env
tts_cache/
scripts/_tts_probe_out.mp3

# Interviewer avatar art — kept out of git (HF rejects raw binaries;
# re-add via Git LFS/Xet when these are actually wired up).
frontend/src/interviewers/*.png
